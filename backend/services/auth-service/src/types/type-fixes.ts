// Re-export type declarations from type-fixes.d.ts
// This file exists to allow importing from '../types/type-fixes' without the .d.ts extension
// No actual implementation is needed as the types are declared in the .d.ts file

// Export an empty object to satisfy module requirements
export {};
